# Toy fixture

`toy.csv` is a small two-dimensional data set used by the YAAAP test suite and
examples. It was generated from the `toy` data distributed by the CRAN
`archetypes` package and is installed as an external CSV fixture rather than as
an exported YAAAP data set.
