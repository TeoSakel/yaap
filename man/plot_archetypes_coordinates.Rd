% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_archetypes_coordinates}
\alias{plot_archetypes_coordinates}
\title{Coordinate Plot For Archetypes}
\usage{
plot_archetypes_coordinates(
  coordinates,
  data = NULL,
  projection = c("none", "pca"),
  archetype_names = NULL,
  show_anames = TRUE,
  args.data.scatter = list(),
  plot = TRUE,
  ...
)
}
\arguments{
\item{coordinates}{Numeric matrix of archetype coordinates. This is the only
required argument.}

\item{data}{Optional numeric matrix of observations to draw behind the
archetypes.}

\item{projection}{Projection to use. \code{"pca"} is only valid when \code{data} is
supplied.}

\item{archetype_names}{Optional labels to draw next to archetype points.}

\item{show_anames}{Logical. Should archetype labels be drawn?}

\item{args.data.scatter}{Named list of graphical arguments for observation
points.}

\item{plot}{Logical. Should the plot be drawn?}

\item{...}{Graphical arguments for archetype points and paths. General plot
window arguments such as \code{main}, \code{xlab}, \code{ylab}, \code{xlim}, \code{ylim}, and \code{asp}
are also honored when drawing the plot.}
}
\value{
Returns a data frame in long format with one row per
point. Columns are the coordinate dimensions, \code{name} (character label),
and \code{archetype} (logical). Data rows come first, archetype rows last.
}
\description{
Plots archetype coordinates, optionally overlaid on observation data. When
\code{data} is supplied and \code{projection = "pca"}, observations and coordinates are
projected together onto the first two principal components before plotting.
}
