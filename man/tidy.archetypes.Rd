% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{tidy.archetypes}
\alias{tidy.archetypes}
\alias{tidy.kernel_archetypes}
\title{Tidy an archetypes model}
\usage{
\method{tidy}{archetypes}(x, matrix = c("coordinates", "coefficients", "compositions"), ...)

\method{tidy}{kernel_archetypes}(x, matrix = c("coordinates", "coefficients", "compositions"), ...)
}
\arguments{
\item{x}{An object of class \code{archetypes} or \code{kernel_archetypes}.}

\item{matrix}{Which component to return: \code{"coordinates"} (K x M archetype
coordinates, default), \code{"coefficients"} (K x N weights of archetypes over
samples), or \code{"compositions"} (N x K weights of samples over archetypes).}

\item{...}{Ignored.}
}
\value{
A tibble. Column names depend on \code{matrix}:
\describe{
\item{\code{"coordinates"}}{\code{archetype}, \code{term}, \code{value} columns (K * M rows).}
\item{\code{"coefficients"}}{\code{archetype}, \code{sample}, \code{value} columns (K * N rows).}
\item{\code{"compositions"}}{\code{sample}, \code{archetype}, \code{value} columns (N * K rows).}
}
}
\description{
Converts an \code{archetypes} model into a tidy long-form tibble.
}
\details{
For \code{kernel_archetypes}, \code{matrix = "coordinates"} returns the
\code{coordinates} matrix (\code{coefficients \%*\% data}) when available, and
emits a warning with an empty tibble otherwise. \code{"coefficients"} and
\code{"compositions"} behave identically to the \code{archetypes} method.
}
\seealso{
\code{\link[=glance.archetypes]{glance.archetypes()}}, \code{\link[=augment.archetypes]{augment.archetypes()}}
}
