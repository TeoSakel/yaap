% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archetypes_nnls.R
\name{archetypes_nnls}
\alias{archetypes_nnls}
\title{Perform Archetypal Analysis using NNLS}
\usage{
archetypes_nnls(
  data,
  K,
  init = "furthest_sum",
  init_args = list(),
  weights = NULL,
  scale = TRUE,
  robust = FALSE,
  tukey_c = 4.685,
  sd_threshold = 1e-06,
  max_iter = 100L,
  tol = 1e-06,
  tol_r2 = 0.9999,
  max_kappa = 1000,
  eps = ifelse(inherits(data, "sparseMatrix"), 0, 1e-08),
  verbose = FALSE,
  ols_solver = c("qr", "ginv", "BFGS"),
  bigM = NULL,
  max_no_update = 5L
)
}
\arguments{
\item{data}{data matrix (rows = samples, columns = dimensions)}

\item{K}{number of archetypes}

\item{init}{function, method string, or numeric coordinate matrix to initialize
archetypes (default: \code{"furthest_sum"}). When a matrix is supplied it must
have dimension \verb{K x ncol(data)}. Rows outside the convex hull of \code{data} are
projected into it with a warning; row names, when present, are used as
archetype names.}

\item{init_args}{list of additional arguments for the initialization function}

\item{weights}{optional vector of sample weights (default: NULL)}

\item{scale}{scaling or metric embedding used before fitting. \code{TRUE} applies
the default z-score preprocessing, \code{FALSE} leaves columns on their original
scale, a positive numeric vector divides columns by user-supplied scale
factors, and a symmetric positive-definite matrix applies the corresponding
feature metric embedding in the original data column space.}

\item{robust}{whether to use Tukey bisquare row reweighting (default: FALSE)}

\item{tukey_c}{tuning constant for Tukey bisquare weights (default: 4.685)}

\item{sd_threshold}{threshold for feature standard deviation to filter
low-variance features (default: 1e-6)}

\item{max_iter}{maximum number of iterations (default: 100)}

\item{tol}{convergence tolerance based on residual sum of squares (default: 1e-6)}

\item{tol_r2}{convergence tolerance based on R^2 (default: 0.9999)}

\item{max_kappa}{maximum condition number for archetypes (default: 1000)}

\item{eps}{small positive number to ensure numerical stability
(default: 0 for sparse input 1e-8 for dense)}

\item{verbose}{whether to print progress messages (default: FALSE)}

\item{ols_solver}{method for solving the OLS problem min_A X = SA (default: "qr")}

\item{bigM}{large constant to enforce simplex constraint, or \code{NULL} to set it
from the dimension-adjusted RMS feature standard deviation after
preprocessing (default: \code{NULL}).}

\item{max_no_update}{maximum consecutive outer iterations with no RSS
improvement before NNLS stops as stalled (default: 5)}
}
\value{
An object of class \code{\link{archetypes}}
}
\description{
Perform Archetypal Analysis using NNLS
}
\details{
NNLS uses the \code{bigM} column to make nonnegative least-squares coefficients
approximately simplex-constrained. If raw NNLS coefficient row sums are far
from one, a warning is emitted because the defensive simplex projection is
materially changing the block solution.
}
\examples{
toy <- read.csv(system.file("extdata", "toy.csv", package = "YAAAP"))
archetypes_nnls(as.matrix(toy), K = 3)

}
\references{
Alcacer, A., Epifanio, I., Mair, S., & Mørup, M. (2025).
A Survey on Archetypal Analysis. \emph{arXiv preprint arXiv:2504.12392}.
\url{https://arxiv.org/abs/2504.12392}
}
