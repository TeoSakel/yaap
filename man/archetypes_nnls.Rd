% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archetypes_nnls.R
\name{archetypes_nnls}
\alias{archetypes_nnls}
\title{Archetypal Analysis using Non-Negative Least Squares}
\usage{
archetypes_nnls(
  x,
  K,
  init = "furthest_sum",
  init_args = list(),
  weights = NULL,
  scale = FALSE,
  robust = FALSE,
  robust_args = list(),
  sd_threshold = 1e-06,
  max_iter = 100L,
  tol = 1e-04,
  tol_r2 = 0.9999,
  max_kappa = 1000,
  eps = ifelse(inherits(x, "sparseMatrix"), 0, 1e-08),
  verbose = FALSE,
  max_no_update = 5L,
  bigM = NULL
)
}
\arguments{
\item{x}{data matrix (rows = samples, columns = dimensions)}

\item{K}{number of archetypes}

\item{init}{initialization method; see \code{\link[=run_aa]{run_aa()}} for available options.}

\item{init_args}{list of additional arguments for the initialization function.}

\item{weights}{optional vector of sample weights (default: NULL)}

\item{scale}{scaling or metric embedding used before fitting; see
\code{\link[=archetypes_pgd]{archetypes_pgd()}} for details (default: FALSE).}

\item{robust}{robust row reweighting selector. See \code{\link[=run_aa]{run_aa()}} for available options
(default: FALSE).}

\item{robust_args}{list of tuning arguments passed to the robust psi function.}

\item{sd_threshold}{threshold for feature standard deviation to filter
low-variance features (default: 1e-6).}

\item{max_iter}{maximum number of iterations (default: 100)}

\item{tol}{convergence tolerance based on residual sum of squares (default: 1e-4)}

\item{tol_r2}{convergence tolerance based on R\eqn{^2} (default: 0.9999)}

\item{max_kappa}{maximum condition-number warning threshold for NNLS
diagnostics (default: 1000). Use \code{Inf} to disable condition-number warnings.}

\item{eps}{small positive number to ensure numerical stability
(default: 0 for sparse input 1e-8 for dense)}

\item{verbose}{whether to print progress messages (default: FALSE)}

\item{max_no_update}{maximum consecutive iterations without improvement
before considering NNLS stalled (default: 5)}

\item{bigM}{large constant to enforce simplex constraint,
or \code{NULL} to set it automatically.}
}
\value{
An object of class \code{archetypes}.
}
\description{
Performs archetypal analysis by iteratively solving the linear systems
definining each of the 3 components (\code{coordinates}, \code{compositions}, \code{coefficients})
using non-negative least-squares (NNLS) to enforce the simplex constraints.
}
\details{
\subsection{NNLS solver}{

Standard AA formulation involves 3 linear systems \eqn{X = SA = S(BX)}, where
\eqn{S} is the \code{compositions} matrix, \eqn{A} is the \code{coordinates} matrix and
\eqn{B} is the \code{coefficients} matrix. \code{archetypes_nnls()} solves each of these
sequentially, via least-squares minimization, keeping the other two matrices
fixed. For the matrices, \eqn{S} and \eqn{B}, that are subject to simplex constraints
(non-negativity and row-sum-to-one), the NNLS solver is used and the sum-to-one constraint
is enforced by adding a penalty term \code{bigM} into the systems involving these matrices.

When \code{bigM = NULL} (default) its value is set automatically based on heuristics.
If it fails to enforce the simplex constraint, warning messages will ask you
to increase it. If the solver is slow or numerically unstable try decreasing it.

Because the solution involves solving linear systems, the condition numbers
of the \code{compositions} and \code{coordinates} matrices are tracked as diagnostics
(\code{k_S} and \code{k_A} respectively). If either of them exceeds \code{max_kappa}, a warning
is issued.

The returned \code{loss} history stores \code{loss} (residual sum-of-squares) as
the best objective seen so far and \code{rloss} as the current iterate objective.
The \code{rloss}, \code{k_S}, and \code{k_A} columns are NNLS diagnostics and are not used
for model selection.
}
}
\examples{
toy <- read.csv(system.file("extdata", "toy.csv", package = "yaap"))
archetypes_nnls(as.matrix(toy), K = 3)

}
\references{
Alcacer, A., Epifanio, I., Mair, S., & Mørup, M. (2025).
A Survey on Archetypal Analysis. \emph{arXiv preprint arXiv:2504.12392}.
\url{https://arxiv.org/abs/2504.12392}
}
\seealso{
\code{\link[=run_aa]{run_aa()}} for the common entry point and full parameter documentation.
}
