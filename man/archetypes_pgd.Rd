% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archetypes_pgd.R
\name{archetypes_pgd}
\alias{archetypes_pgd}
\title{Archetypes Analysis using Projected Gradient Descent}
\usage{
archetypes_pgd(
  data,
  K,
  init = "furthest_sum",
  init_args = list(),
  weights = NULL,
  robust = FALSE,
  tukey_c = 4.685,
  sd_threshold = 1e-04,
  max_iter = 500L,
  tol = 1e-06,
  tol_r2 = 0.9999,
  max_kappa = 1000,
  eps = ifelse(inherits(data, "sparseMatrix"), 0, 1e-08),
  verbose = FALSE,
  delta = 0,
  pseudo_pgd = TRUE,
  step_size = 1,
  max_iter_optimizer = 10L,
  step_shrinkage = 0.5
)
}
\arguments{
\item{data}{data matrix (rows = samples, columns = dimensions)}

\item{K}{number of archetypes}

\item{init}{function, method string, or numeric coordinate matrix to initialize
archetypes (default: \code{"furthest_sum"}). When a matrix is supplied it must
have dimension \verb{K x ncol(data)}. Rows outside the \code{delta}-relaxed convex
hull of \code{data} are projected into it with a warning; row names, when
present, are used as archetype names.}

\item{init_args}{list of additional arguments for the initialization function}

\item{weights}{optional vector of sample weights (default: NULL)}

\item{robust}{whether to use Tukey bisquare row reweighting (default: FALSE)}

\item{tukey_c}{tuning constant for Tukey bisquare weights (default: 4.685)}

\item{sd_threshold}{threshold for feature standard deviation to filter low-variance features (default: 1e-4)}

\item{max_iter}{maximum number of iterations (default: 500)}

\item{tol}{convergence tolerance based on residual sum of squares (default: 1e-6)}

\item{tol_r2}{convergence tolerance based on R^2 (default: 0.9999)}

\item{max_kappa}{maximum condition number for archetypes (default: 1000)}

\item{eps}{small positive number to ensure numerical stability
(default: 0 for sparse input 1e-8 for dense)}

\item{verbose}{whether to print progress messages (default: FALSE)}

\item{delta}{maximum allowed relaxation of archetypes convexity constraint (default: 0)}

\item{pseudo_pgd}{whether to use pseudo projected gradient descent (default: TRUE)}

\item{step_size}{initial step size for the gradient descent (default: 1.0)}

\item{max_iter_optimizer}{maximum iterations for the line search optimizer (default: 10)}

\item{step_shrinkage}{factor to reduce step size if no improvement during line search (default: 0.5)}
}
\value{
An object of class \code{\link{archetypes}}
}
\description{
Archetypes Analysis using Projected Gradient Descent
}
\examples{
toy <- read.csv(system.file("extdata", "toy.csv", package = "YAAAP"))
archetypes_pgd(as.matrix(toy), K = 3)

}
\references{
Mørup, M., & Hansen, L. K. (2012).
Archetypal analysis for machine learning and data mining.
\emph{Neurocomputing}, 80, 54-63. \url{https://dx.doi.org/10.1016/j.neucom.2011.06.033}
}
