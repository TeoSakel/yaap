% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archetypes_pgd.R
\name{archetypes_pgd}
\alias{archetypes_pgd}
\title{Archetypes Analysis using Projected Gradient Descent}
\usage{
archetypes_pgd(
  x,
  K,
  init = "furthest_sum",
  init_args = list(),
  weights = NULL,
  scale = FALSE,
  robust = FALSE,
  robust_args = list(),
  sd_threshold = 1e-06,
  max_iter = 100L,
  tol = 1e-06,
  tol_r2 = 0.9999,
  eps = ifelse(inherits(x, "sparseMatrix"), 0, 1e-08),
  verbose = FALSE,
  missing = any(is.na(x)),
  delta = 0,
  pseudo_pgd = TRUE,
  step_size = 1,
  max_iter_optimizer = 10L,
  step_shrinkage = 0.5,
  max_no_update = 5L
)
}
\arguments{
\item{x}{data matrix (rows = samples, columns = dimensions)}

\item{K}{number of archetypes}

\item{init}{initialization method; see \code{\link[=run_aa]{run_aa()}} for available options.}

\item{init_args}{list of additional arguments for the initialization function.}

\item{weights}{optional vector of sample weights (default: NULL)}

\item{scale}{scaling or metric embedding used before fitting. \code{FALSE}
(default) leaves columns on their original scale, \code{TRUE} applies
z-score preprocessing, a positive numeric vector divides columns by
user-supplied scale factors, and a symmetric positive-definite matrix
applies the corresponding feature metric embedding in the original data
column space.}

\item{robust}{robust row reweighting selector. Use \code{FALSE} for ordinary
squared error, \code{TRUE} for \code{"psi.bisquare"}, a MASS psi function name,
or a custom psi function. See \code{\link[MASS:rlm]{MASS::rlm()}} for psi details; \code{method = "MM"} is not supported because it is not applicable to AA.}

\item{robust_args}{list of tuning arguments passed to the robust psi function.}

\item{sd_threshold}{threshold for feature standard deviation to filter
low-variance features (default: 1e-6)}

\item{max_iter}{maximum number of iterations (default: 100)}

\item{tol}{convergence tolerance based on residual sum of squares (default: 1e-6)}

\item{tol_r2}{convergence tolerance based on R^2 (default: 0.9999)}

\item{eps}{small positive number to ensure numerical stability
(default: 0 for sparse input 1e-8 for dense)}

\item{verbose}{whether to print progress messages (default: FALSE)}

\item{missing}{whether to fit the missing-data PGD objective. When \code{TRUE},
only observed entries are optimized; dense \code{NA} values are treated as
missing and sparse structural zeros are treated as missing.}

\item{delta}{maximum allowed relaxation of archetypes convexity constraint (default: 0)}

\item{pseudo_pgd}{whether to use pseudo projected gradient descent (default: TRUE)}

\item{step_size}{initial step size for the gradient descent (default: 1.0)}

\item{max_iter_optimizer}{maximum iterations for the line search optimizer (default: 10)}

\item{step_shrinkage}{factor to reduce step size if no improvement during line search (default: 0.5)}

\item{max_no_update}{maximum consecutive outer iterations with no accepted
line-search update before PGD stops as stalled (default: 5)}
}
\value{
An object of class \code{archetypes}
}
\description{
Fits an archetypal analysis (AA) model by minimising the reconstruction error
\eqn{\|X - SA\|_F^2}, where \eqn{S} is the composition matrix and \eqn{A} is
the archetype coordinates via projected gradient descent (PGD).
}
\details{
\subsection{Model family}{

Three arguments change \emph{which} objective is optimised and therefore the
statistical family of the fitted model:
\itemize{
\item \strong{\code{delta}} relaxes the convexity constraint on the archetypes.  With the
default \code{delta = 0}, every archetype is a convex combination of observed
data points (i.e., it lies inside or on the boundary of the data convex
hull).  Setting \code{delta > 0} allows archetypes to step outside the hull by
up to a factor \eqn{1 + \delta} and inside by up to \eqn{1 - \delta} —
useful when the true extremes are absent from the data due to truncation or
censoring.  Use with care: large values remove the interpretability
guarantee that archetypes are representable as extreme prototypes, and the
uniqueness result of Theorem 1 in Mørup & Hansen (2012) no longer holds.
\item \strong{\code{robust}} switches the loss from ordinary squared error to an
iteratively re-weighted version based on MASS-style psi row weights.
Use \code{TRUE} for \code{"psi.bisquare"}, or pass \code{"psi.huber"}, \code{"psi.hampel"},
or a custom psi function with tuning values in \code{robust_args}. See
\code{\link[MASS:rlm]{MASS::rlm()}} for psi details. \code{method = "MM"} from \code{MASS::rlm()} is not
supported because MM estimation is not directly applicable to the AA
objective. Robust fitting is incompatible with \code{missing = TRUE}.
\item \strong{\code{missing}} activates the missing-data objective described in Section 3.5
of Mørup & Hansen (2012).  When \code{TRUE}, only observed entries contribute to
the loss and the archetype profiles are normalised entry-wise so that each
archetype is a weighted average of the \emph{observed} values of the data points
that define it.  For dense matrices, \code{NA} marks missing entries; for sparse
\code{sparseMatrix} inputs, structural zeros are treated as missing.  The
default is \code{any(is.na(x))}, so missing-data mode is activated automatically
when the input contains \code{NA}s.  For kernel and probabilistic variants of AA
see \code{vignette("non-gaussian-aa", package = "yaap")}.
}
}

\subsection{Algorithm mechanics and tuning}{

The solver alternates gradient steps for \eqn{S} and \eqn{B} with an inner
line search of up to \code{max_iter_optimizer} steps (default 10) that shrinks the
step size by \code{step_shrinkage} (default 0.5) until the objective decreases.
The outer loop repeats for at most \code{max_iter} iterations (default 100).

When \code{pseudo_pgd = TRUE} (the default), the gradients are projected onto the
tangent space of the \eqn{\ell_1} unit sphere before the simplex projection
step.  This is the "pseudo-PGD" variant of Mørup & Hansen (2012), which
removes the radial gradient component and thereby prevents the step from
increasing the \eqn{\ell_1} norm before projection.  In practice it leads to
larger effective steps and faster convergence; set \code{pseudo_pgd = FALSE} to
use the plain projected gradient if needed.

\code{step_size} sets the initial step size for both \eqn{S} and \eqn{B} updates
(and for \eqn{\alpha} when \code{delta > 0}).  If no step in the inner line
search is accepted for \code{max_no_update} consecutive outer iterations (default
5), the solver declares the iterate stalled and stops early.  Step sizes are
also reduced after a failed outer iteration, so in practice the solver
self-tunes step sizes down as it approaches a local minimum; there is usually
no need to change \code{step_size} unless the default is far from the optimal
scale for a particular dataset.

Convergence is declared when the relative decrease in RSS between successive
accepted updates falls below \code{tol} (default 1e-6) \emph{or} \eqn{R^2} exceeds
\code{tol_r2} (default 0.9999).
}

\subsection{Preprocessing}{

Before fitting, features with standard deviation below \code{sd_threshold}
(default 1e-6) are dropped to avoid ill-conditioning; their values are
restored as column means in the output.  The \code{scale} argument controls the
feature metric: \code{FALSE} (default) leaves columns on their original scale, \code{TRUE}
applies column-wise z-scoring, a positive numeric vector divides
each column by the supplied factor, and a symmetric positive-definite matrix
defines a full quadratic feature metric — see
\code{vignette("non-gaussian-aa", package = "yaap")} for the metric-AA use
case.  Sample-level \code{weights} re-weight rows in the loss but do not affect
the preprocessing step.
}
}
\examples{
toy <- read.csv(system.file("extdata", "toy.csv", package = "yaap"))
archetypes_pgd(as.matrix(toy), K = 3)

}
\references{
Mørup, M., & Hansen, L. K. (2012).
Archetypal analysis for machine learning and data mining.
\emph{Neurocomputing}, 80, 54-63. \doi{10.1016/j.neucom.2011.06.033}
}
