% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_archetypes.R
\name{step_archetypes}
\alias{step_archetypes}
\title{Archetypal Analysis Preprocessing Step for recipes}
\usage{
step_archetypes(
  recipe,
  ...,
  role = "predictor",
  trained = FALSE,
  num_comp = 3L,
  delta = 0,
  fit_method = "pgd",
  options = list(),
  reconstruct = FALSE,
  keep_original_cols = FALSE,
  res = NULL,
  col_names = NULL,
  seed = sample.int(100000L, 1L),
  skip = FALSE,
  id = recipes::rand_id("archetypes")
)
}
\arguments{
\item{recipe}{A recipe object.}

\item{...}{One or more selector functions to choose which variables are
affected by the step. See \code{\link[recipes:selections]{recipes::selections()}} for more details.}

\item{role}{Role for the \emph{new} columns produced by this step.
Default \code{"predictor"}.}

\item{trained}{Logical. Has the step been trained (i.e. has \code{prep()} been
called)?  Set by \code{prep()} — do not change manually.}

\item{num_comp}{Number of archetypes K (default \code{3L}). Tunable via
\code{\link[tune:tune]{tune::tune()}}.}

\item{delta}{Convexity penalty for the pgd solver (default \code{0}). Ignored
when \code{fit_method != "pgd"}. Tunable via \code{\link[tune:tune]{tune::tune()}}.}

\item{fit_method}{Solver to use: \code{"pgd"} (default), \code{"nnls"}, \code{"fw"}, or \code{"paa"}.}

\item{options}{A named list of additional arguments passed to \code{\link[=run_aa]{run_aa()}}
(e.g. \code{nrep}, \code{max_iter}, \code{scale}). These are not validated here.}

\item{reconstruct}{If \code{TRUE}, \code{bake()} also appends columns named
\verb{rec_<original_col>} containing the low-rank reconstruction
\code{S \%*\% Z} (compositions times archetype coordinates).}

\item{keep_original_cols}{Should the original predictor columns be retained
after baking? Default \code{FALSE}.}

\item{res}{The fitted \link[=archetypes_pgd]{archetypes} object. \code{NULL}
before \code{prep()}, set automatically by \code{prep()}.}

\item{col_names}{Character vector of selected column names. Set by \code{prep()}.}

\item{seed}{Integer seed for reproducible fitting. Evaluated once at
construction time (default: a random integer).}

\item{skip}{Should the step be skipped during \code{bake()} on the test set?
Default \code{FALSE}.}

\item{id}{A character string that identifies this step in the recipe.}
}
\value{
An updated recipe with the new step appended.
}
\description{
\code{step_archetypes()} creates a \emph{specification} of a recipe step that projects
numeric predictors onto a K-archetype simplex.  During \code{prep()} the
archetypes are fitted once (with optional random-restart via \code{nrep}); during
\code{bake()} new data are projected onto the learnt simplex.
}
\details{
\code{step_archetypes()} is \strong{not} supported for \code{method = "directional"} or
\code{method = "kernel"}.

The \code{num_comp} and \code{delta} parameters support \code{\link[tune:tune]{tune::tune()}} for
hyperparameter search.
}
\examples{
\dontrun{
library(recipes)
data(iris)
rec <- recipe(Species ~ ., data = iris) |>
    step_archetypes(all_numeric_predictors(), num_comp = 3L) |>
    prep(training = iris)
bake(rec, new_data = iris)
}

}
