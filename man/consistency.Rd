% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consistency.R
\name{consistency}
\alias{consistency}
\alias{consistency.archetypes}
\title{Consistency Between Archetypal Analysis Fits}
\usage{
consistency(x, y, ...)

\method{consistency}{archetypes}(
  x,
  y,
  what = c("compositions", "coefficients", "coordinates"),
  data = NULL,
  ...
)
}
\arguments{
\item{x}{An object.}

\item{y}{A fitted archetypes object to compare with \code{x}.}

\item{...}{Arguments passed to methods.}

\item{what}{Component to compare: compositions, coefficients, or coordinates.}

\item{data}{Optional input data used as the variance reference for coordinate
consistency. Defaults to \code{x$data}.}
}
\value{
A numeric scalar.
}
\description{
Measures how similar two archetypal analysis fits are by comparing their
memberships, archetype definitions, or archetype locations.
}
\details{
Consistency is useful when checking whether an AA solution is stable across
random starts, resampled data, or nearby model choices. A high score means the
two fits describe the data in a similar way, even if archetype labels have
changed. Compare \code{compositions} to ask whether samples are assigned to the
same archetypal mixtures, \code{coefficients} to ask whether archetypes are built
from the same samples, and \code{coordinates} to ask whether the archetype
locations are close in the input space.

For \code{what = "compositions"} or \code{what = "coefficients"}, the two components
are treated as row-stochastic membership matrices and compared with
normalized mutual information (NMI). For matrices \code{X} and \code{Y}, the joint
distribution is \code{Pxy = crossprod(X, Y) / nrow(X)}. Mutual information is
computed from \code{Pxy} and its row and column marginals, then normalized as
\code{2 * MI(X, Y) / (MI(X, X) + MI(Y, Y))}. This makes the score insensitive to
permutations of the archetype labels.

For \code{what = "coordinates"}, archetypes are greedily matched by repeatedly selecting the
closest remaining pair across the two fits, then a similarity-style score is
computed by scaling the mean matched squared distances by the average
squared pairwise distance between observations in \code{data}. This denominator
is computed through the equivalent identity \code{2 * sum(colVars(data))},
avoiding explicit pairwise distances. The score is useful for checking
whether two fits place archetypes in similar regions of the input space.
Values can be negative when matched archetypes are farther apart than the
typical data-to-data squared distance. It returns \code{NA} when \code{x} has more
archetypes than \code{y}, because not every archetype in \code{x} can be matched.
}
\references{
J. L. Hinrich, S. E. Bardenfleth, R. E. Røge, N. W. Churchill, K. H.
Madsen, and M. Mørup, "Archetypal analysis for modeling multisubject
fMRI data," \emph{IEEE Journal of Selected Topics in Signal Processing},
vol. 10, no. 7, pp. 1160-1171, 2016.
}
