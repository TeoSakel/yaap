% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consistency.R
\name{consistency}
\alias{consistency}
\alias{consistency.archetypes}
\title{Consistency Between Archetypal Analysis Fits}
\usage{
consistency(x, y, ...)

\method{consistency}{archetypes}(
  x,
  y,
  what = c("compositions", "coefficients", "coordinates"),
  data = NULL,
  ...
)
}
\arguments{
\item{x}{An object.}

\item{y}{A fitted archetypes object to compare with \code{x}.}

\item{...}{Arguments passed to methods.}

\item{what}{Component to compare: compositions, coefficients, or coordinates.}

\item{data}{Optional input data used as the variance reference for coordinate
consistency. Defaults to \code{x$data}.}
}
\value{
A numeric scalar.
}
\description{
Consistency Between Archetypal Analysis Fits
}
\details{
For \code{what = "compositions"} or \code{what = "coefficients"}, consistency is
computed as normalized mutual information (NMI) between the two row-stochastic
weight matrices. For matrices \code{X} and \code{Y}, the joint distribution is
\code{Pxy = crossprod(X, Y) / nrow(X)}. Mutual information is computed from \code{Pxy}
and its row and column marginals, and normalized as
\code{2 * MI(X, Y) / (MI(X, X) + MI(Y, Y))}.

For \code{what = "coordinates"}, consistency is defined when \code{K_x <= K_y}.
Archetypes in \code{x} are greedily matched to their nearest unmatched archetype
in \code{y} by squared Euclidean distance, and the score is
\code{1 - mean(d2) / mean(matrixStats::colVars(data))}. When \code{K_x > K_y}, the
score is \code{NA}.
}
\references{
J. L. Hinrich, S. E. Bardenfleth, R. E. Røge, N. W. Churchill, K. H.
Madsen, and M. Mørup, "Archetypal analysis for modeling multisubject
fMRI data," \emph{IEEE Journal of Selected Topics in Signal Processing},
vol. 10, no. 7, pp. 1160-1171, 2016.
}
