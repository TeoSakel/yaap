% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archetypes_ensemble.R
\name{tune_archetypes}
\alias{tune_archetypes}
\title{Tune Archetypal Analysis Models}
\usage{
tune_archetypes(
  data,
  fit_method = c("pgd", "nnls", "kernel", "directional", "paa"),
  K,
  eval = stats::AIC,
  eval_name = NULL,
  nrep = 1L,
  direction = NULL,
  ...
)
}
\arguments{
\item{data}{Data passed to \code{\link[=run_aa]{run_aa()}}.}

\item{fit_method}{Fitting method passed as \code{method} to \code{\link[=run_aa]{run_aa()}}.}

\item{K}{Integer vector with the numbers of archetypes to fit.}

\item{eval}{Evaluation function. Defaults to \code{\link[stats:AIC]{stats::AIC()}}.}

\item{eval_name}{Optional name for the evaluation metric column.}

\item{nrep}{Number of replicates per grid point.}

\item{direction}{Preferred optimization direction, \code{"minimize"} or
\code{"maximize"}. Defaults to \code{"minimize"} for AIC.}

\item{...}{Additional arguments passed to \code{\link[=run_aa]{run_aa()}}. Atomic vectors of
length greater than one are expanded as tuning dimensions. Plain list
arguments are kept fixed; wrap a list in \code{\link[=I]{I()}} to expand it as a tuning
dimension.}
}
\value{
An object of class \code{archetypes_ensemble}.
}
\description{
Fits a grid of archetypal analysis models over numbers of archetypes,
replicates, and tuning arguments, evaluates each fit, and stores the result
as an archetypes ensemble.
}
