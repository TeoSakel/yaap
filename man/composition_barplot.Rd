% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archetype_class.R
\name{composition_barplot}
\alias{composition_barplot}
\title{Stacked barplot of composition weights}
\usage{
composition_barplot(
  x,
  cluster_rows = FALSE,
  cluster_cols = FALSE,
  distance = NULL,
  distance_rows = "euclidean",
  distance_cols = "euclidean",
  linkage = "complete",
  col = NULL,
  legend = TRUE,
  border = NA,
  ...
)
}
\arguments{
\item{x}{Numeric matrix or data frame. Rows are samples and columns are
archetypes. Rows should contain non-negative composition weights.}

\item{cluster_rows, cluster_cols}{Logical values or \code{hclust} objects. When
\code{TRUE}, rows or columns are reordered by hierarchical clustering. Row
clustering is computed on \code{compositions::cdt(compositions::acomp(x))}.}

\item{distance}{Optional distance metric used for both row and column
clustering when \code{distance_rows} or \code{distance_cols} are not supplied.}

\item{distance_rows, distance_cols}{Distance metrics used when clustering
rows or columns. Values may be any method accepted by \code{\link[stats:dist]{stats::dist()}},
\code{"correlation"}, a function that returns a \code{dist} object, or a precomputed
\code{dist} object. Row distances are computed after the cdt transform.}

\item{linkage}{Linkage method passed to \code{\link[stats:hclust]{stats::hclust()}}.}

\item{col}{Optional vector of colors, one per archetype. Defaults to a
qualitative HCL palette.}

\item{legend}{Logical. Should an archetype legend be drawn?}

\item{border}{Border color for the stacked bar segments.}

\item{...}{Additional graphical parameters passed to \code{\link[graphics:barplot]{graphics::barplot()}}.}
}
\value{
Invisibly returns a list with the reordered matrix, row and column
orders, and clustering objects.
}
\description{
Draws a horizontal stacked barplot for a matrix-like set of composition
weights, with rows interpreted as samples and columns interpreted as
archetypes or other compositional parts.
}
\details{
This display is intended for small composition matrices, roughly of tens of
samples and/or archetypes. For many samples or many archetypes, a heatmap is
usually easier to read.
}
