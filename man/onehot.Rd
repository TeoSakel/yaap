% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projections.R
\name{onehot}
\alias{onehot}
\alias{onehot.default}
\alias{onehot.factor}
\alias{onehot.character}
\title{One-hot encode a vector}
\usage{
onehot(ind, sparse = FALSE, ...)

\method{onehot}{default}(ind, sparse = FALSE, nc = NULL, ...)

\method{onehot}{factor}(ind, sparse = FALSE, ...)

\method{onehot}{character}(ind, sparse = FALSE, ...)
}
\arguments{
\item{ind}{A vector to encode. Can be numeric/integer, factor, or character.}

\item{sparse}{Logical; if \code{TRUE}, return a memory-efficient sparse matrix.}

\item{...}{Additional arguments passed to the specific input handler.}

\item{nc}{Number of columns (for numeric input only). If \code{NULL}, uses \code{max(ind)}.}
}
\value{
A one-hot encoded matrix:
\itemize{
\item dense \code{matrix} when \code{sparse = FALSE}
\item sparse \code{Matrix::dgCMatrix} when \code{sparse = TRUE}
}
}
\description{
\code{onehot()} converts a vector into a \strong{one-hot encoded} matrix: each distinct
value corresponds to a column, and each row has a \code{1} in the matching column
and \code{0} elsewhere. This is useful for turning categorical values into numbers
for modeling and data preprocessing.
}
\details{
\itemize{
\item \strong{Numeric/integer input:} values are treated as 1-based column indices.
The number of columns (\code{nc}) defaults to the largest value in the input,
but you can set \code{nc} manually if you need more columns (e.g., reserving
space for unseen categories).
\item \strong{Factor input:} each factor level becomes a column; the number of columns
is fixed to the number of levels and the columns are named by those levels.
\item \strong{Character input:} automatically converted to a factor before encoding.
}

Missing values produce rows of all zeros.
}
\section{Methods (by class)}{
\itemize{
\item \code{onehot(default)}: Numeric indices

\item \code{onehot(factor)}: Factor method: \code{nc} is fixed to the number of levels

\item \code{onehot(character)}: Character method: delegates to \code{factor}

}}
\examples{
# Numeric vector: columns correspond to indices 1..max(ind)
onehot(c(1, 2, 1, 3))

# Factor: columns follow factor levels (and are named)
f <- factor(c("red", "blue", "red", NA, "green"), levels = c("red","blue","green"))
onehot(f)

# Character: automatically treated as a factor
onehot(c("cat","dog","cat"), sparse = TRUE)

# Reserving extra columns for future/unseen categories (numeric input only)
onehot(c(1, 2, 1), sparse = FALSE, nc = 5)

}
