% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archetype_class.R
\name{archetypes}
\alias{archetypes}
\alias{anames}
\alias{anames<-}
\alias{anames.archetypes}
\alias{anames<-.archetypes}
\title{Archetype Analysis Result Object}
\usage{
archetypes(
  coordinates,
  coefficients,
  compositions,
  slack = 0,
  loss = NULL,
  converged = TRUE,
  call = NULL,
  data = NULL,
  init = NULL,
  family = "gaussian",
  weights = NULL
)

anames(x)

anames(x) <- value

\method{anames}{archetypes}(x)

\method{anames}{archetypes}(x) <- value
}
\arguments{
\item{coordinates}{Numeric matrix (K x M) giving the archetype coordinates
in the original feature space. \code{K} is the number of archetypes, \code{M} is the
number of features.}

\item{coefficients}{Numeric matrix (K x N) giving the weights that express
each archetype as a linear combination of the N samples in the input data.}

\item{compositions}{Numeric matrix (N x K) giving the barycentric
coordinates of each sample in the archetype space (how much each archetype
contributes to each sample).}

\item{slack}{Non-negative numeric scalar or vector giving the allowed
relaxation of coefficient row sums away from 1.}

\item{loss}{Data frame containing per-iteration metrics}

\item{converged}{Logical. Whether the optimization converged.}

\item{call}{The matched function call that created the object (defaults to \code{match.call()}).}

\item{data}{Original data used for fitting. Usually a numeric matrix (N x M),
but class-specific entry points may store the original input object. If
supplied, residuals are computed against fitted values.}

\item{init}{Optional numeric matrix (K x M) with the initial archetype
coordinates before optimization.}

\item{family}{Observation family. Defaults to \code{"gaussian"}.}

\item{weights}{Optional numeric vector of sample weights used during
fitting. When present, it is used by \code{residuals(..., type = "pearson")}.}

\item{x}{An archetype analysis result.}

\item{value}{Character vector with one name per archetype.}
}
\value{
An object of class \code{archetypes}, which is a list with components:
\itemize{
\item \code{coordinates} - (K x M) archetype coordinates.
\item \code{coefficients} - (K x N) archetype weights on samples.
\item \code{compositions} - (N x K) sample weights on archetypes.
\item \code{init} - optional initial coordinates.
\item \code{loss} - data frame of per-iteration metrics.
\item \code{converged} - logical convergence flag.
\item \code{data} - optional original data.
\item \code{residuals} - \code{data - compositions \%*\% coordinates}
\item \code{call} - the matched call.
}

\code{anames()} returns a character vector. The replacement method returns \code{x}
with names updated consistently across archetype coordinates, coefficients,
compositions, and initial coordinates when present.
}
\description{
Creates an S3 object of class \code{archetypes} holding the fitted archetypes,
their decomposition of the data, and diagnostic information from the fitting
process.

Get or set the names of archetypes in an archetype analysis result.
}
\details{
Let \code{X} be the original data matrix (N x M).

The fitted values are:

\if{html}{\out{<div class="sourceCode">}}\preformatted{  X_hat = compositions \%*\% coordinates
  coordinates = coefficients \%*\% X
}\if{html}{\out{</div>}}

\code{compositions} is always a row-stochastic matrix (each row sums to 1).
\code{coefficients} is typically a row-stochastic matrix as well unless the
constraint is relaxed (\code{slack} > 0) in which case the archetypes can lay
outside the convex hull of the data.
\subsection{Invariants and Dimensions}{
\itemize{
\item \verb{nrow(coordinates) == ncol(compositions) == nrow(coefficients) == K}
\item \verb{nrow(compositions) == ncol(coefficients) == N}
\item \code{rowSums(compositions) == 1}
\item If \code{data} is supplied: \code{dim(data) == c(N, M)} where \code{M = ncol(coordinates)}
}
}

\subsection{Archetype names}{

Archetype labels are stored as row names on \code{coordinates} and \code{coefficients},
and as column names on \code{compositions}. Use \code{anames()} and \verb{anames<-()} to get
or set them consistently:

\if{html}{\out{<div class="sourceCode">}}\preformatted{anames(fit)
anames(fit) <- c("A", "B", "C")
}\if{html}{\out{</div>}}
}
}
\examples{
# anames(fit)
# anames(fit) <- c("A", "B", "C")

}
\seealso{
\code{\link[=plot.archetypes]{plot.archetypes()}}, \code{\link[=predict.archetypes]{predict.archetypes()}}, \code{\link[=residuals.archetypes]{residuals.archetypes()}}, \code{\link[=fitted.archetypes]{fitted.archetypes()}}
}
