% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{aa_init}
\alias{aa_init}
\title{Archetypal Analysis Initialization Functions}
\usage{
aa_init(
  X,
  K,
  method = "furthest_sum",
  sparse = inherits(X, "sparseMatrix"),
  m = NULL,
  batch_size = NULL,
  ...
)
}
\arguments{
\item{X}{a numeric matrix (rows = samples, columns = dimensions)}

\item{K}{number of archetypes to be initialized}

\item{method}{initialization method. One of \code{"uniform_archetypes"},
\code{"furthest_first"}, \code{"kmeans_pp"}, \code{"furthest_sum"}, \code{"coreset_initfn"},
\code{"aa_pp"}, or \code{"aa_pp_mc"} (default: \code{"furthest_sum"}).}

\item{sparse}{whether \code{B} should be a sparse matrix (default: same as X)}

\item{m}{optional batch size for coreset initialization}

\item{batch_size}{optional batch size for MCMC approximation of the AA++ initialization}

\item{...}{additional arguments (used for compatibility with user-defined functions)}
}
\value{
a named list containing two matrices: the archetype coordinates \code{A}
and a row-stochastic matrix \code{B} such that \code{A = B \%*\% X}.
}
\description{
Various initialization methods for Archetypal Analysis (see Details).
}
\details{
\code{aa_init()} runs the selected initialization method and formats the result
as a named list containing the archetype coordinates \code{A} and row-stochastic
matrix \code{B}. User-defined initialization functions passed directly to
\code{archetypes_nnls()} or \code{archetypes_pgd()} must follow that same interface.

The following initialization methods are available:
\itemize{
\item \code{"uniform_archetypes"}: selects uniformly at random \code{K} archetypes from \code{X}.
\item \code{"furthest_first"}: selects the first archetype randomly and then greedily
selects the point furthest from the current set of archetypes.
\item \code{"kmeans_pp"}: a soft version of \code{furthest_first} where points are sampled
in proportion to their distance from the current set of archetypes instead
of greedily picking the furthest point every time.
\item \code{"furthest_sum"}: selects the first archetype randomly and then greedily
selects the next archetypes that maximizes the sum of distances of all
points from the current set of archetypes (see Mørup & Hansen 2012).
\item \code{"coreset_initfn"}: initializes archetypes using a coreset of size \code{m} sampled
from the data matrix \code{X} and then applies \code{furthest_sum} on the coreset
(see Mair & Brefeld 2019).
\item \code{"aa_pp"}: AA++ is a probabilistic initialization method similar to \verb{kmeans++}
but instead using the distances to the current set of archetypes it uses
distances to their convex hull (see Mair & Sjölund 2023).
\item \code{"aa_pp_mc"}: MCMC approximation of AA++ initialization. Each archetype is
sampled by performing AA++ on a sub-sample of size \code{batch_size} from the
data matrix \code{X} (see Mair & Sjölund 2023).
}
}
\references{
Mørup, M., & Hansen, L. K. (2012).
Archetypal analysis for machine learning and data mining.
Neurocomputing, 80, 54-63.
\url{https://doi.org/10.1016/j.neucom.2011.06.033}

Mair, S., & Sjölund, J. (2023).
Archetypal analysis++: Rethinking the initialization strategy.
\url{https://arxiv.org/abs/2301.13748}

Mair, S., & Brefeld, U. (2019).
Coresets for archetypal analysis.
Advances in Neural Information Processing Systems, 32.
\url{https://proceedings.neurips.cc/paper_files/paper/2019/file/7f278ad602c7f47aa76d1bfc90f20263-Paper.pdf}
}
