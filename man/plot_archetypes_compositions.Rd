% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_archetypes_compositions}
\alias{plot_archetypes_compositions}
\title{Composition Plot For Archetypes}
\usage{
plot_archetypes_compositions(
  compositions,
  plot = TRUE,
  cluster_rows = FALSE,
  cluster_cols = FALSE,
  distance = NULL,
  distance_rows = "euclidean",
  distance_cols = "euclidean",
  linkage = "complete",
  col = NULL,
  legend = TRUE,
  border = NA,
  ...
)
}
\arguments{
\item{compositions}{Numeric matrix or data frame. Rows are samples and
columns are archetypes. Rows should contain non-negative composition
weights.}

\item{plot}{Logical. Should the plot be drawn?}

\item{cluster_rows, cluster_cols}{Logical values, one of \code{"PC1"} or
\code{"AOP"}, or \code{hclust} objects. When \code{TRUE}, rows or columns are reordered
by hierarchical clustering. \code{"PC1"} orders rows by their first principal
component score and columns by their first principal component loading.
\code{"AOP"} orders rows by the angle of their PC1/PC2 scores and columns by
the angle of their PC1/PC2 loadings.}

\item{distance}{Optional distance metric used for both row and column
clustering when \code{distance_rows} or \code{distance_cols} are not supplied.}

\item{distance_rows, distance_cols}{Distance metrics used when clustering
rows or columns. Values may be any method accepted by \code{\link[stats:dist]{stats::dist()}},
\code{"correlation"}, a function that returns a \code{dist} object, or a precomputed
\code{dist} object. Row distances are computed after the centered log-ratio
transform.}

\item{linkage}{Linkage method passed to \code{\link[stats:hclust]{stats::hclust()}}.}

\item{col}{Optional vector of colors, one per archetype. Defaults to a
qualitative HCL palette.}

\item{legend}{Logical. Should an archetype legend be drawn?}

\item{border}{Border color for the stacked bar segments.}

\item{...}{Additional graphical parameters passed to \code{\link[graphics:barplot]{graphics::barplot()}}.}
}
\value{
Returns a data frame in long format with one row per
sample/archetype pair. Columns are \code{sample} (factor ordered by the applied
clustering), \code{archetype} (factor ordered by the applied clustering), and
\code{weight} (numeric).
}
\description{
Draws a horizontal stacked barplot for a matrix-like set of composition
weights, with rows interpreted as samples and columns interpreted as
archetypes or other compositional parts.
}
