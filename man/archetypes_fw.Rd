% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archetypes_fw.R
\name{archetypes_fw}
\alias{archetypes_fw}
\title{Archetypal Analysis using Frank-Wolfe Updates}
\usage{
archetypes_fw(
  x,
  K,
  init = "furthest_sum",
  init_args = list(),
  weights = NULL,
  scale = FALSE,
  sd_threshold = 1e-06,
  max_iter = 100L,
  tol = 1e-04,
  tol_r2 = 0.9999,
  eps = ifelse(inherits(x, "sparseMatrix"), 0, 1e-08),
  verbose = FALSE,
  nrep = 1L,
  max_iter_optimizer = 10L,
  max_no_update = 5L
)
}
\arguments{
\item{x}{data matrix (rows = samples, columns = dimensions)}

\item{K}{number of archetypes}

\item{init}{initialization method; see \code{\link[=run_aa]{run_aa()}} for available options.}

\item{init_args}{list of additional arguments for the initialization function.}

\item{weights}{optional vector of sample weights (default: NULL)}

\item{scale}{scaling or metric embedding used before fitting; see
\code{\link[=archetypes_pgd]{archetypes_pgd()}} for details (default: FALSE).}

\item{sd_threshold}{threshold for feature standard deviation to filter
low-variance features (default: 1e-6)}

\item{max_iter}{maximum number of outer iterations (default: 100)}

\item{tol}{convergence tolerance based on residual sum of squares (default: 1e-4)}

\item{tol_r2}{convergence tolerance based on R\eqn{^2} (default: 0.9999)}

\item{eps}{small positive number to ensure numerical stability
(default: 0 for sparse input, 1e-8 for dense)}

\item{verbose}{whether to print progress messages (default: FALSE)}

\item{nrep}{number of random restarts; the best fit is returned (default: 1)}

\item{max_iter_optimizer}{number of Frank-Wolfe inner steps used for each
alternating update of the composition and coefficient matrices (default: 10)}

\item{max_no_update}{maximum consecutive outer iterations with no accepted
Frank-Wolfe update before stopping as stalled (default: 5)}
}
\value{
An object of class \code{archetypes}.
}
\description{
Fits an archetypal analysis (AA) model using greedy Frank-Wolfe steps.
}
\details{
Like \code{\link[=archetypes_pgd]{archetypes_pgd()}}, this method minimises the ordinary squared-error AA
objective \eqn{\|X - SBX\|_F^2}. The difference is that each update moves
directly toward a simplex vertex selected by the current gradient, rather
than taking a gradient step and projecting the result back to the simplex.
This projection-free update is the rapid AA strategy proposed by Bauckhage
et al. (2015), motivated by the fact that the composition and coefficient
columns each lie in a standard simplex.

The method is often attractive when a fast, sparse approximation is preferred
over the smoother projected-gradient trajectory. The paper also frames AA as
an autoencoder because the factorisation maps \eqn{X} through archetypal
coefficients and back to a reconstruction \eqn{XBA}.

\code{method = "fw"} supports ordinary Euclidean fitting with optional scaling,
sample weights, random restarts, and sparse matrix input. Robust and
missing-data objectives are not supported by this solver.
}
\examples{
toy <- read.csv(system.file("extdata", "toy.csv", package = "yaap"))
archetypes_fw(as.matrix(toy), K = 3)

}
\references{
Bauckhage, C., Kersting, K., Hoppe, F., & Thurau, C. (2015).
Archetypal analysis as an autoencoder. \emph{Workshop New Challenges in Neural
Computation}, 8-15.
}
\seealso{
\code{\link[=run_aa]{run_aa()}} for the common entry point and full parameter documentation.
}
