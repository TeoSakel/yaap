% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{augment.archetypes}
\alias{augment.archetypes}
\alias{augment.kernel_archetypes}
\title{Augment data with composition weights from an archetypes model}
\usage{
\method{augment}{archetypes}(x, data = NULL, ...)

\method{augment}{kernel_archetypes}(x, data = NULL, ...)
}
\arguments{
\item{x}{An object of class \code{archetypes} or \code{kernel_archetypes}.}

\item{data}{Optional data frame or matrix to augment. If \code{NULL}, uses the
data stored inside \code{x} (if available). For \code{archetypes} objects, passing
new data triggers \code{\link[=predict.archetypes]{predict.archetypes()}} to compute compositions.}

\item{...}{Passed to \code{\link[=predict.archetypes]{predict.archetypes()}} when \code{data} is provided and
\code{x} is an \code{archetypes} object.}
}
\value{
A tibble with all columns from \code{data} plus one column per archetype named
\code{.A1}, \code{.A2}, etc. (dot-prefixed \code{anames(x)}).
}
\description{
Adds per-sample archetype composition columns to the original data.
}
\details{
For \code{kernel_archetypes}, \code{data} is used only to convert the stored data to
a tibble; compositions always come from the stored \code{x[["compositions"]]}
since projecting new samples requires the original Gram matrix.
}
\seealso{
\code{\link[=tidy.archetypes]{tidy.archetypes()}}, \code{\link[=glance.archetypes]{glance.archetypes()}}
}
